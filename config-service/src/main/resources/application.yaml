server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/MiguelAlvarez91/microservicios
          search-paths: config-data
  application:
    name: config-service